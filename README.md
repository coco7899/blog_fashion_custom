# 네이버 블로그 자동화 대시보드

관심분야 키워드만 입력하면 **글감 수집 → AI 글 작성 → 이미지 선별 → 네이버 블로그 자동 발행**까지 완전 자동으로 진행되는 로컬 웹 대시보드입니다.

## 필요 조건

- Node.js 18+
- Claude Code CLI 로그인 상태 (`claude` 명령 사용 가능) — AI 호출은 `claude -p` 헤드리스 모드로 **구독 요금제**를 사용합니다 (API 종량제 아님)

## 실행

```bash
npm install
npm start
```

브라우저에서 `http://localhost:3000` 접속.
첫 실행 시 Playwright Chromium이 없으면 자동으로 설치합니다.

## 사용 흐름

1. **네이버 로그인** — 우측 상단 로그인 버튼 → 브라우저 창이 뜨면 직접 로그인 (2단계 인증 포함). 세션은 `data/session/naver-state.json`에 저장되어 재사용됩니다. 자동화 코드는 아이디/비밀번호를 다루지 않습니다.
2. **키워드 입력 → 글감 찾기** — 네이버 뉴스·인기 블로그 글을 수집하고 AI가 글감 5개를 추천.
3. **글감 선택** — 선택하는 순간 참고자료 수집 → AI 글 재작성 → 관련 기사 이미지 다운로드 → AI가 이미지를 직접 보고 적합성 판정 → 스마트에디터 ONE에 서식(소제목/굵게/인용구/구분선/이미지) 적용 → **자동 발행**까지 무개입 진행.
4. **발행 이력** — 완료된 글 링크와 읽기 전용 미리보기 제공.

처음에는 "비공개 발행 (테스트용)" 옵션으로 결과물을 확인한 뒤 공개 발행을 사용하는 것을 권장합니다.

## 주의

- 뉴스 기사 이미지를 사용하므로 발행 글에 항상 `사진 출처`가 표기되지만, 상업적 이용 등 저작권 이슈는 사용자 책임입니다.
- 네이버 에디터 UI가 변경되면 발행 자동화가 실패할 수 있습니다. 실패 시 `data/drafts/<id>/publish-error.png` 스크린샷을 확인하세요. 셀렉터는 `src/publisher.js` 상단 `SEL`에 모여 있습니다.
